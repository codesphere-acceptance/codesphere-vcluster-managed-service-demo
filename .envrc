#!/usr/bin/env bash
# Auto-activates the pinned mise toolchain and loads local Codesphere API creds
# (CS_API / CS_TEAM_ID / CS_TOKEN) when .env.local exists. Run `direnv allow`
# once to trust this file.
use mise
dotenv_if_exists .env.local
